/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    // Autorise l'utilisation d'un mode hors-ligne côté client (service worker à ajouter en V2)
  },
};

export default nextConfig;
